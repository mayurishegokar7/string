#include <stdio.h>
#include <string.h>
int main()
{
   char s1[40],s2[40];
   printf("enter string 1:");
   fgets(s1,40,stdin);
   printf("enter string 2:");
   fgets(s2,40,stdin);
   strcat(s1,s2);
   printf("%s",s1);
   return 0;
}
